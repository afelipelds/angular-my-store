const username: string = 'altair6';

const sum = (a: number, b: number) => {
  return a + b;
};

sum(2, 938949);

class Person {
  constructor(public age: number, public lastName: string) {}
}

const andres = new Person(29, 'Eslava');
andres.age;
